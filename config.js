/*
 * ┌─────────────────────────────────────────────────────────────────────────┐
 * │  EDIT THIS FILE — it is the only file you change to publish your links.    │
 * └─────────────────────────────────────────────────────────────────────────┘
 *
 * Fill in the two URLs below, commit, and push. The page regenerates the QR
 * codes and download buttons automatically. Leave a URL as an empty string
 * ("") to show a "Coming soon" state for that platform (no broken QR).
 *
 * iosUrl     — your UNLISTED App Store link. You get this AFTER the app is
 *              approved and you've enabled Unlisted distribution. It looks like:
 *                https://apps.apple.com/app/botc/id6500000000
 *              (Until then, leave it "" — the iOS card shows "Coming soon".)
 *
 * androidUrl — a direct link to the .apk file people download and install.
 *              Easiest: attach the APK to a GitHub Release on this repo and use
 *              its download URL, e.g.
 *                https://github.com/<you>/botc-download/releases/latest/download/botc.apk
 *              (See README.md → "Hosting the Android APK".)
 */
window.BOTC_CONFIG = {
  appName: 'BOTC',
  tagline: 'A Storyteller companion for Blood on the Clocktower',
  version: '1.0.0',

  iosUrl: '',
  androidUrl: '',

  // Shown in the footer and on the privacy page. Use an address you check.
  supportEmail: 'chas.nolte@aviture.us.com',
}
